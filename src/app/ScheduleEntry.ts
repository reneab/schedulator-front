export class ScheduleEntry {
    // id: number;
    // time: string;
    // teacher: string;
    // batch: string;
    // room: string;
    // subject: string;

    constructor(public id: string, public time: string, public teacher: string,
                public batch: string, public room: string, public subject: string) {}
}
